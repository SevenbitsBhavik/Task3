install all the dependencies like web3 mysql 
add your infuralink, senderData(address & private_key), receiver_address.
also i have made 2 function, one for signing the transaction and other for sending the transaction,
we can run both at the same time or one after the other.